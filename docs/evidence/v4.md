# V4 independent retry — PASS

Verifier: `/root/v4_retry`, fresh context. Date: 2026-09-12. Scope: S4 and the approved same-chat owner audience addendum. Both prior blocking findings are repaired. No new blocking S4 findings. S5 may begin; this is not installation or live delivery approval evidence.

## Frozen artifacts and reproducible checks

All five S2, 14 S3 and 13 S4 manifest entries matched before and after the independent checks. S4 manifest SHA-256: `d87fac029d5b8f7b8683326f552c551ae356875b94d94d3a977b2fc685713fc3`. No frozen source was edited. `git fsck --full` exited 0, with dangling objects only and no corruption.

- `.venv/bin/python -m unittest discover -s tests -v`: 61 tests passed.
- `.venv/bin/python .private/v4-retry/verify.py`: independent supplemental fixtures and read-only metadata checks passed; sanitized results in `.private/v4-retry/results.json`. Both files are mode0600 under a mode0700 directory.
- `.venv/bin/python scripts/probe_channels.py --socket`: exact Slack bot/app identity, existing owner DM, actual scope metadata and Socket Mode handshake passed; the client closed with no event handler or send attached. Messages query-only, initial highwater/backlog exclusion and latest iMessage/SMS/RCS frozen owner metadata passed.

## Repairs verified

Independent fixtures place malformed archives, oversized archives, malformed reactions and invalid timestamps ahead of a valid request. Each valid later request is returned, its history excludes invalid content, the cursor advances and the next poll does not replay the poison record. A separate actual SQLite missing-table error propagates and leaves the cursor unchanged. Source inspection confirms reaction/system filtering precedes decoding in ingress and history; sanitized numeric failure counts retain no bodies, identifiers or exception text. Missing dependencies and database failures are not swallowed as record errors.

I independently read the current `com.apple.imservice.ids.iMessage.plist` ActiveAccounts/OnlineAccounts intersection, correlated those GUIDs with outgoing account/chat-login metadata, and checked the configured own aliases against that source. The latest outgoing record for each of iMessage, SMS and RCS has the exact frozen account/GUID pair and independently verified own account and chat-login aliases. Native Messages service IDs additionally match iMessage and SMS GUIDs. The RCS GUID does not occur in the native service list: its evidence is the current local outgoing database correlation with independently anchored own aliases, not native service-list confirmation.

That RCS provenance is sufficient for this S4 setup gate because it combines the OS-maintained outgoing flag, two previously anchored owner metadata fields and an exact operator-frozen GUID; neither message text nor an incoming sender supplies the trust anchor. It does not establish live RCS send support or all migrated account identities. Historical unmatched GUIDs remain untrusted; source inspection and fixtures confirm no runtime identity learning and rejection of altered account, GUID, login or incoming status.

## Complete S4 boundary coverage

Source review and fixtures cover Slack owner/nonowner DMs, mixed-case textual/native mentions, group/thread destinations, exact app/workspace/bot identity, attachment references, self/duplicate events, first-start cutoff and bot sender arguments. Independent supplemental cases check mention boundaries and duplicate message/app_mention events. A separate live users.info request confirmed the configured owner's exact workspace/user, active nonbot identity. Observed scopes support visible public/private channel and one-to-one DM reads plus bot chat writes; mpim read/history scopes are absent, consistent with the documented group-DM limit.

Messages fixtures cover direct/group IDs, read-only write rejection, Unicode attributedBody, attachments, bounded exact-chat history, owner impersonation rejection, self/duplicate filtering, first-start highwater, and shared-chat batch boundaries. I independently verified both configured self-chats have direct style, exactly one participant and verified own login/participant aliases. Only these configured self-chats auto-trigger; ordinary outgoing third-party chats need a mention. Same-chat owner-group behavior is enabled in mode0600 installation configuration and covered by the passing S3/S4 suite.

The fixed-source AppleScript sender passes exact chat/body as arguments; injected-runner fixtures exercise destination and failure behavior without sends. Slack sends use the verified bot client and preserve destination/thread. The separately packaged channel plugin provides guidance, contains no credentials, and grants no execution authority. Persistent receivers and delivery remain host code. S2/S3 artifacts stayed unchanged.

## Remaining downstream evidence

S5 still must integrate the durable queue, metadata-only delivery ledger/self-reply callback, recovery and final response delivery. Actual event subscription coverage, live inbound receipt, final bot/Messages delivery, RCS transport behavior and deployed service health are not proven by the handshake or fixtures; the approved later gates retain these requirements. No raw private message bodies, attachment contents or credentials were printed or stored in ordinary reports. No messages were sent, no continuous service started, no legacy source/archive was used, and no push, merge or publication occurred.
