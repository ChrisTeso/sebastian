# V2 — PASS

Fresh independent verifier: `/root/v2`. Date: 2026-09-12. Scope: S2 runtime selection only. S3 may begin; no later implementation, service installation, channel delivery, performance or production security gate is covered by this result.

## Candidate and integrity

Verified branch `codex/sebastian-clean-rebuild`, HEAD `74c7336ce430988777a76c6e7400bb2c0f40b673`. All five candidate artifacts matched `docs/evidence/s2-artifact-hashes.json` before and after verification; candidate source was not changed. `git fsck --full` exited 0, reporting dangling objects but no corruption.

Selected binary `/Applications/ChatGPT.app/Contents/Resources/codex` reports `codex-cli 0.154.0-alpha.6.2`; independently computed SHA-256 `ecad78dbf98adb89ec475edac86630406cbe59d9f3070b17d88065f136b94bcb` matches the candidate.

## Independent reproduction

The verifier created new synthetic fixtures, a new persistent App Server thread, and separate private evidence. It reused only the inspected transport helper, overriding its evidence directory before constructing any RPC client. The full clean-rebuild plan and graph protocol were read. Risk-review checks focused on actual tool results, authentication, runtime mismatch and weak proof.

| Requirement | Result and evidence |
| --- | --- |
| Harmless local read | PASS. Actual `commandExecution` ran `cat fixture.txt`, exited 0 and returned `V2-COPPER-HERON-918204`. This is a new verifier fixture. |
| Existing logged-in Chrome session | PASS after a bounded recovery. Native CUA inventory worked; selecting an existing GitHub tab twice failed with CDP focus timeouts. A temporary GitHub tab in the existing Chrome profile then returned an actual accessibility tree containing the signed-in Dashboard and `Open user navigation menu`. Native `probeTab.close()` completed. No login, credential inspection, private-message page, or shell/browser substitute was used. |
| Harmless Mac app control | PASS. Native Calculator accessibility state first showed the prior result, then actual UI input changed the expression to `13×17` and display to `221`. |
| Existing authenticated service-plugin read | PASS. `codex_apps` tool `github.get_profile`, empty arguments, completed with `readOnlyHint: true`, no tool error, and structured profile fields. Private field values are omitted here. |
| Same conversation continuity | PASS. Subsequent no-tool turn returned `AMBER-FOX-624809`, the new marker from the preceding verifier turn. |
| Active cancellation and fresh-process reconnect | PASS. A `sleep 45` command was observed in progress; `turn/interrupt` produced terminal status `interrupted`. The first App Server process was closed, a fresh process resumed the same thread, and a no-tool turn recovered the original marker. A further fresh process queried `thread/backgroundTerminals/list`, returning an empty data list. |

The browser failure is retained in evidence rather than erased by the successful retry. This establishes capability in the current host/profile, not a guarantee that every existing tab can always be attached. Later runtime recovery must surface failures and use supported bounded recovery.

## Authentication and fallback assessment

`account/read` independently reported `type: chatgpt`, `planType: pro`. The inspected helper starts App Server with only HOME, PATH, TMPDIR, USER, LOGNAME, LANG and SHELL. No calling desktop task/session/pipe identity or API key is injected. No external-token login, credential extraction, second reasoning runtime, or CLI substitute supplied any capability. The isolated probe used the current native managed login.

The saved Agents API request explicitly included both the CUA and GitHub plugin roots. Its environment-connected event, native computer-use results, API item history and cleanup record were inspected. The service-plugin turn made no `get_profile` call and returned unavailable. Therefore that candidate did not demonstrate the required existing connected-service access. This justifies the tested fallback; it does not establish that every Agents API plugin or separately authenticated MCP is unsupported. The disposable API session was recorded deleted and its executor exited 0. V2 did not rerun those API calls or access the saved restricted key.

## Dependencies, costs and retention

The runtime decision accurately records the desktop/native helper, Chrome profile/extension, existing login, OS permissions, version sensitivity and Mac availability limits. Actual CUA and connected GitHub calls validate those integrations at the stated binary version. S2 does not prove availability while asleep or signed out.

Official App Server documentation confirms managed ChatGPT authentication and thread interruption/resumption interfaces. Plugin documentation places MCP authentication in the session environment; a bundle alone is not connected-auth proof. Current pricing documentation states local/cloud usage shares the plan allowance and API-key usage follows API rates. The decision appropriately avoids a fixed per-message price or unlimited-use claim. Durable native session history is acknowledged separately from Sebastian logs; production retention enforcement remains an explicit S5/S6 obligation.

Sources checked live: [App Server](https://learn.chatgpt.com/docs/app-server), [Agents API plugins](https://developers.openai.com/api/docs/guides/agents-api/tools/plugins), [pricing](https://learn.chatgpt.com/docs/pricing), [computer use](https://learn.chatgpt.com/use-cases/use-your-computer-with-codex).

## Reproduction and raw evidence

Run from `/Users/teso/Code/Mini Me`:

- `python3 .private/v2/verify.py` — fresh fixture/thread, file, continuity, GitHub, Chrome, Calculator, cancellation and process reconnect.
- `python3 .private/v2/browser_retry.py` — resumes only the verifier thread, checks background terminals and verifies signed-in UI through a temporary existing-profile tab, then closes it.
- `git fsck --full`
- `/Applications/ChatGPT.app/Contents/Resources/codex --version`
- Recompute each file SHA-256 and compare with `docs/evidence/s2-artifact-hashes.json`; recompute binary SHA-256 against the value above.

Private raw evidence: `.private/v2/results.json`, `events.json`, `reconnect-events.json`, `browser-retry.json`, `browser-retry-events.json`, and `evidence-hashes.json`. Scripts and fixtures are under the same private directory. Actual completed MCP call results, accessibility trees, command output and protocol events were inspected, not merely the runtime's final answers. `git check-ignore` confirms verifier scripts/results are excluded. The private directory is mode 0700; generated evidence uses restrictive umask. No external messages or service writes were made. Verifier App Server processes exited; no existing browser tab or app process was closed.

No unresolved S2 blocker remains. All subsequent gates remain required.
