# V5 independent verification — PASS

Verifier: fresh agent `/root/v5_retry`, 2026-09-12. Scope: complete approved S5, including the owner same-group audience clarification. Read repository AGENTS, full approved rebuild plan, graph protocol, and risk-review skill. No frozen source was edited. PASS unlocks S6 only.

## Previously blocking failure repaired

Independently drove **actual Engine, Sources, SlackAdapter, Policy and Ledger** with synthetic Slack provider records, a fixture runtime and in-memory send transport. An image provider call failed once after authenticated message rehydration. The first Engine attempt left the job queued with no model call and no delivery. After advancing the fixture clock, the next attempt retrieved the image, invoked the model exactly once, delivered three bounded final-answer parts to the original owner group/thread and completed the job. Reaccepting the identical metadata produced no further work or delivery. The loader was invoked twice. `Sources._slack_event` now creates a fresh validating adapter on every rehydration; durable processing dedup belongs to Ledger rather than an ephemeral adapter seen set.

Command: `.venv/bin/python .private/v5-retry/integrated.py`. Evidence: `.private/v5-retry/integrated-result.json`. This integrated reproduction goes beyond the repository's direct Sources retry regression test. It resolves the blocker in `v5-first-fail.md`.

## Checks independently executed

- `.venv/bin/python -m unittest discover -s tests -q`: **104 tests passed**, 0.267 seconds. Evidence: `.private/v5-retry/tests.txt`.
- `git fsck --full`: exit 0, dangling objects only; no integrity error. Evidence: `.private/v5-retry/fsck.txt`.
- `.venv/bin/python scripts/probe_s5_runtime.py`: actual selected native runtime correctly interpreted a generated red PNG; remembered a synthetic conversation token after killed-process reconnect and SessionRuntime reconstruction; observed an active native turn, accepted cancellation, raised RuntimeFailure and joined the canceled worker; preserved earlier conversation continuity on a later explicit request; completed two concurrent requests. All eight functional flags true. Evidence: `.private/v5-retry/runtime-output.json`.
- `.venv/bin/python scripts/probe_s5_engine.py`: production SessionRuntime through production Engine to an in-memory transport delivered the expected final, suppressed duplicate processing and preserved the fixture group/thread. This run took 4.935 seconds overall; this single functional observation is **not** S6 latency evidence. The script's accumulated ledger contains three confirmed jobs from separate invocations; this invocation asserted exactly one send in its fresh in-memory transport. Evidence: `.private/v5-retry/engine-output.json`.
- `.venv/bin/python .private/v5-retry/native.py`: actual restricted SessionRuntime resumed a prior synthetic conversation, recalled its token, refused the requested synthetic file/browser/personal-memory escalation, and did not return the private canary. Observed completed item types were userMessage and final agentMessage only, with zero tool events. A separate authorized owner request read only the verifier's synthetic canary and returned its exact value; observed one commandExecution plus userMessage and final agentMessage. Evidence: `.private/v5-retry/native-result.json` and native-output.txt. No private user files or browser state were read.

## Requirement review

- SessionRuntime composes the previously verified native AppServer; no second reasoning runtime or OpenClaw execution dependency was found. Owner tools retain actual host permissions. Restricted profile configuration, pre-turn MCP inventory rejection, dynamic-tool authorization and complete profile/session fingerprints remain enforced outside the model. Fresh restricted resume was exercised above; the full suite also covers profile changes, wrong-scope tool calls, unexpected MCP inventory, mixed senders and private-session isolation.
- Current and preceding images are loaded from authenticated attachment references. Source inspection and executed tests confirm account/chat/thread matching, prior-event/time checks, ten-message history bounds, four-image response bounds, size/type limits, Slack HTTPS host/redirect constraints, and Messages exact GUID/chat membership with symlink-safe opening. Image input uses the actual native image schema and is cleared after each turn, including failure.
- The process-global desktop mutex serializes SessionRuntime calls; executed fixtures observe maximum one active connection call and thread-local image separation. Fresh native concurrency, active cancellation and reconnect passed. Deadline propagation covers lock wait, initialization, RPC and turn processing. Errors discard a poisoned transport; uncertain model actions are not automatically replayed. Cancellation does not imply rollback.
- AppServer extracts only final_answer agent messages; Engine performs channel formatting and sends only the final or a sanitized actionable failure. Executed fixtures cover progress filtering and balanced long code fences in both channels; the independent integrated run verified a three-part final preserved the owner group/thread. The owner addendum is applied, rather than requiring a rejected private-chat continuation.
- Queue/delivery storage contains metadata and keyed body fingerprints. Executed tests cover duplicate/checkpoint atomicity, bounded retries, lease/stale transitions, no retry after runtime start, provider uncertainty, partial-send abandonment, restart reconciliation, already-confirmed response retirement, exact receipt scope, ambiguous matches and self-echo suppression. No uncertain original action/delivery is replayed. Body absence and owner-only storage checks passed.
- Native Codex retains transcripts separately and may retain image/tool content. This is explicitly documented and is not zero retention. Messages local receipt correlation is not exactly-once or remote receipt/read proof.

## Frozen artifacts and boundaries

Every recorded S2/S3/S4/S5 artifact hash matched before and after verification; both snapshots are under `.private/v5-retry/`. Manifest SHA-256 values:

- S2: `ab93fbb64dbd18f1e7c6a031db544ece365d543d4a27042a835950c0a1d7d4a7`
- S3: `750853f78586295c824f796014603d48bfac4704c573e0a3c09076e15a568278`
- S4: `d87fac029d5b8f7b8683326f552c551ae356875b94d94d3a977b2fc685713fc3`
- S5 repaired: `8ae420636b8b5168decad7a1f0afa500a476c367947bd2ad45e0cf0ed53afe8c`

No remaining S5 blocker was found. Synthetic fixture files and metadata are owner-only. Native processes created by these probes closed normally. No live channel sends, continuous service activation, provider configuration mutation, push, publication or subscription change occurred. S6 full failure/latency/privacy review and S7 installation plus live owner inbound/delivery evidence remain pending; they are not implied by this PASS.
