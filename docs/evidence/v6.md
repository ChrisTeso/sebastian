# V6 independent verification — PASS

Verifier: `/root/v6_complete`, fresh context (`fork_turns: none`). Date: 2026-09-12. Read actual AGENTS.md, approved clean-rebuild S6 and audience addendum, graph protocol and risk-review checklist. No implementation edits. This supersedes the physical-sleep blocker in `v6-before-sleep-blocked.md` and unlocks S7; it does not claim installation or live channel completion.

## Physical recovery independently verified

Read macOS `pmset -g log` as bytes and decoded with replacement, printing only event types/timestamps. OS records actual **sleep 15:35:55, wake 15:36:16 PDT** on September 12. Strict UTF-8 decoding independently reproduced `UnicodeDecodeError`, confirming the collector's reported failure mechanism. No new sleep, administrator prompt, power change or channel send was performed by this verifier.

Inspected `.private/s6-recovery/physical-04usmm61/results.json`, actual SQLite metadata and the exact native synthetic session identified by its persisted metadata. The queued synthetic event was accepted at **15:35:43.972**, before sleep, and remains complete with zero retries. The physical collector recorded zero source fetches and zero runtime calls, with only one fixture interruption notice. The fixture stale threshold is10 seconds: it could already have expired just before sleep; this is proof of refusing an old queued action after the physical cycle, not proof that expiry first occurred during sleep. The production600-second threshold is separately reproduced in the recovery matrix.

The exact native transcript is0600. Inspected only this known synthetic transcript, reducing output to timestamps and token-presence booleans: initial token at **15:35:41.451**, post-wake answer at **15:38:10.471**. The initial collector closed its owned runtime in cleanup after the log-decode error. Therefore this proves persisted native conversation recovery after sleep plus runtime reopening, not survival of an uninterrupted process or an in-flight desktop action.

Independently reopened that actual metadata with SessionRuntime and requested the remembered synthetic token without supplying it. Exact answer matched, native session ID remained unchanged, and owned runtime closed. This additional continuation took **3.532s**, including **0.025s** startup. Evidence: `.private/v6/physical-independent.json`. The original collector's4.791s continuation is a separate observation. Original physical evidence was not overwritten. Physical evidence files have zero group/other permission bits and the SQLite artifacts contain neither synthetic private-request nor final-body canary.

## Checks independently rerun

- `.venv/bin/python -m unittest discover -s tests -v`: **109 PASS**,1.674s. Includes policy/isolation, provider failures, queue/restart, cancellation, serialization, images, privacy and native transport fixtures.
- `git fsck --full`: exit0, dangling objects only.
- `.venv/bin/python scripts/check_privacy.py`: exit0 before and after probes; actual credential/config/state/restricted paths safe, zero ordinary-artifact credential matches, zero private files tracked, recovery archive owner-only and outside workspace.
- `umask 077` then `.venv/bin/python scripts/probe_recovery.py --native`: exit0. New independent evidence `.private/s6-recovery/run-lltkrj_3/results.json`.

The new recovery run reproduces all eight Engine/Ledger scenarios: fresh/duplicate startup, two temporary retrieval errors with1s/2s bounded retries then success, exhausted retrieval errors with no runtime execution, runtime failure with no replay,601-second stale request with zero fetches/runtime calls, running-job restart interruption, partial-delivery restart preserving uncertain/abandoned parts without replay, and completed-delivery restart with no extra send. All finish as expected. Owned-child suspension/resumption independently passed in1.279s; this is explicitly a process fixture, separate from the OS physical evidence above.

Actual native fresh/warm continuation, explicit reconnect after killing only the probe-owned idle executor, and close/reopen against persisted session metadata all passed. A real allowlisted synthetic local-document tool read returned the independently checked code in **6.192s**, including6.192205s runtime and zero startup. This is one bounded tool observation, not a tool-heavy percentile distribution. All owned runtimes were cleaned up.

Temporary network loss is injected at the source/provider boundary using actual orchestration; it does not claim a live Slack outage. Service restart is actual Engine/Ledger reopening; installed LaunchAgent restart belongs to S7. Failures remain actionable and stale/interrupted actions are not blindly replayed.

## Performance evidence rechecked

The prior independent native benchmark remains unchanged: `.private/s6-latency/1789251868245310000/results.json`, SHA256 `a26280b69b8abc5b9667f705b648634cc084855724125db1519cf2186b8ad28b`. Read raw21 rows, independently recalculated **20 warm** median **3.733s**, nearest-rank p95 **4.991s**, and cold **4.077s**. All answers correct and no completed tool items. Both original targets, median under5s and p95 under10s, pass. A second20-call sample was unnecessary because benchmark/runtime code hashes remain unchanged.

Independently checked nonnegative timings, nested total/model/startup intervals, and each stage median against raw rows. Warm medians: fixture ingress0.000763s; queue wait0.001534s; fixture fetch0.000141s; native startup0s; model/runtime/tool interval3.726572s; fixture delivery0.003101s. These stages can overlap and medians must not be added. Provider ingress/delivery are synthetic; model execution uses the actual native owner runtime. No live channel latency claim is made.

## Privacy/security review and remaining deployment requirements

Reviewed actual runtime policy, SessionRuntime, Engine, checker and probe sources alongside prior V6 evidence. Restricted policy removes native environments/capability roots, configured MCP/apps/plugins, personal memory and project instructions and fails closed on unexpected MCP inventories. Exact policy fingerprints separate native sessions; full tests re-exercise sender/workspace/audience boundaries. Native execution remains Chris's OS identity under the previously verified per-thread controls, not a separate OS user/container. Provider secrets are excluded from inherited runtime environment; stderr/provider diagnostics are suppressed and exceptions sanitized. Durable queue stores metadata/HMACs rather than raw messages. Recovery archive was not opened or reused as implementation.

Owner group replies retain owner tools and the originating audience under Chris's explicit addendum; unrelated sensitive-data avoidance is not a universal semantic disclosure filter. Credential matching checks current ordinary working artifacts, not all Git history or every possible private string.

Native transcripts are retained separately. Both benchmark transcripts and the exact physical synthetic transcript are0600. Five older harmless S5 synthetic transcripts remain0644 as previously disclosed. **S7 must set umask077 before state creation/native spawn and verify a newly deployed transcript0600.** This requirement is not waived. No global permissions were changed. Installation, automatic restart, live owner Slack/Messages receipts and delivery, and an owner computer/Chrome request remain S7/V7 requirements.

## Frozen artifacts

All **56 files** match S2–S6 manifests before and after verification (5/14/13/15/9 files). S2–S5 manifests are unchanged from prior V6. Current manifest hashes:

- S2 `ab93fbb64dbd18f1e7c6a031db544ece365d543d4a27042a835950c0a1d7d4a7`
- S3 `750853f78586295c824f796014603d48bfac4704c573e0a3c09076e15a568278`
- S4 `d87fac029d5b8f7b8683326f552c551ae356875b94d94d3a977b2fc685713fc3`
- S5 `8ae420636b8b5168decad7a1f0afa500a476c367947bd2ad45e0cf0ed53afe8c`
- S6 `972fb73bbdccc34c6835e608b6a007ed128280daf18ae40eb8d45e019a6cf8eb`

No actionable S6 defect or remaining S6 blocker found within the stated proof boundaries. Changes by this verifier are this report and ignored synthetic evidence/native continuation metadata only. No deployment, live sends, provider/global configuration changes, push or merge.
